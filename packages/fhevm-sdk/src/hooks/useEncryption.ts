/**
 * useEncryption hook
 *
 * Hook for encrypting values with FHE.
 * Provides encryption functions with loading states and error handling.
 * This is a re-export from the React adapter for better organization.
 */

export { useEncrypt as useEncryption } from '../adapters/react';
